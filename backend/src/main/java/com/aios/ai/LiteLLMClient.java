package com.aios.ai;

import org.springframework.stereotype.Component;

/**
 * Placeholder for direct LiteLLM HTTP client operations (health checks, model listing).
 * Spring AI's OpenAI-compatible client handles chat routing via {@link AIGatewayService}.
 */
@Component
public class LiteLLMClient {
}
